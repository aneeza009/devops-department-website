name: QA CI Pipeline

on:
  push:
    branches:
      - release

jobs:
  qa-build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Validate Website Files
        run: |
          echo "QA validation running..."
          ls

      - name: Build Docker Image
        run: |
          docker build -t department-website .